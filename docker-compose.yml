services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: nextjs_app
    ports:
      - "3000:1004"
    environment:
      - NODE_ENV=production
    restart: always